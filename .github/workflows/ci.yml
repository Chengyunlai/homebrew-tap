name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  generator:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Check generator
        run: |
          python -m unittest discover -s tests -v
          python -m compileall -q scripts tests

  formula:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Install Formula
        run: |
          brew tap Chengyunlai/tap "${GITHUB_WORKSPACE}"
          brew install Chengyunlai/tap/ops-agent
      - name: Test installed commands
        shell: bash
        run: |
          brew test Chengyunlai/tap/ops-agent
          version="$(sed -n 's/^  version "\(.*\)"/\1/p' Formula/ops-agent.rb)"
          test "$(ops-agent --version)" = "ops-agent ${version}"
          test "$(ops_agent --version)" = "ops-agent ${version}"
